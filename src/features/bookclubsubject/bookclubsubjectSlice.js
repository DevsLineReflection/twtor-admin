import { createSlice } from '@reduxjs/toolkit'

const initialState = {}

const bookclubsubjectSlice = createSlice({
    name: 'bookclubsubject',
    initialState,
    // reducers: {},
})

// export const { userLoggedIn, userLoggedOut } = userSlice.actions
export default bookclubsubjectSlice.reducer
