import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const karmapointSlice = createSlice({
  name: "karmapoint",
  initialState,
  // reducers: {},
});

// export const { userLoggedIn, userLoggedOut } = userSlice.actions
export default karmapointSlice.reducer;
